# ── Core LangChain ────────────────────────────────────────────────────────────
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
langchain-core>=0.3.0

# ── OpenAI SDK (pinned to v1 to align with langchain-openai) ─────────────────
openai>=1.50.0

# ── Vector store ─────────────────────────────────────────────────────────────
faiss-cpu>=1.8.0

# ── PDF extraction ────────────────────────────────────────────────────────────
pypdf>=4.0.0

# ── Sample PDF creation ───────────────────────────────────────────────────────
reportlab>=4.0.0

# ── Markdown → HTML ───────────────────────────────────────────────────────────
markdown>=3.6.0

# ── HTML → PDF (pure Python, cross-platform) ─────────────────────────────────
xhtml2pdf>=0.2.16

# ── Tokenisation helper used by LangChain splitters ──────────────────────────
tiktoken>=0.7.0

# ── LangSmith tracing ─────────────────────────────────────────────────────────
langsmith>=0.1.0

# ── Optional document ingestion formats ───────────────────────────────────────
python-docx>=1.1.0   # .docx support (Word documents)
openpyxl>=3.1.0      # .xlsx support (Excel spreadsheets)
# ── FastAPI gateway and rate limiting ──────────────────────────────────────────
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.9   # Required by FastAPI for File/Form upload routes
slowapi>=0.1.9             # Rate limiting middleware

# ── URL/media ingestion extras ────────────────────────────────────────────────
youtube-transcript-api>=0.6.2
# ── Environment variables ─────────────────────────────────────────────────────
python-dotenv>=1.0.0
