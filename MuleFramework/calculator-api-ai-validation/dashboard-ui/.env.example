# Backend URL for production (set this in Vercel environment variables)
# Leave blank for local development (Vite proxy handles /api/* → localhost:8000)
VITE_API_URL=https://mule-ai-validation.onrender.com
