env: "dev"

http:
  listener:
    host: "0.0.0.0"
    port: "8081"

api:
  baseUri: "http://localhost:8081"

logging:
  level: "DEBUG"

security:
  clientId: "dev-client-id"
  jwt:
    issuer: "https://auth-dev.example.com"
    audience: "calculator-api-dev"
