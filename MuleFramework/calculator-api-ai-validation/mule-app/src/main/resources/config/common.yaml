api:
  name: "calculator-api"
  version: "v1"
  basePath: "/calculator/v1"

http:
  listener:
    host: "0.0.0.0"
    port: "8081"

rate:
  limit:
    requestsPerMinute: 100

logging:
  level: "INFO"

security:
  jwt:
    issuer: "https://auth.example.com"
    audience: "calculator-api"
